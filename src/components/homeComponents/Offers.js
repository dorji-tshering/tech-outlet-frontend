import React from 'react'
import { NavLink } from 'react-router-dom'

const Offers = ({ routes }) => {
    return (
        <div>
            <NavLink to="/templates">offers</NavLink>
        </div>
    )
}

export default Offers
